encoding  utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

source "Downstream_Compile.th"

export model -fmt survex -o "Downstream_Compile-th.3d"

export map -proj plan -layout local -o "Downstream_Compile-plan.pdf"

export map -proj extended -layout local -o "Downstream_Compile-ee.pdf"
