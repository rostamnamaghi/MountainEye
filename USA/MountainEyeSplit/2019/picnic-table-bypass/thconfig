encoding utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

source "picnic-table-bypass.th"

export model -fmt survex -o "picnic-table-bypass-th.3d"

# export map -proj plan -layout local -o "picnic-table-bypass-plan.pdf"

# export map -proj extended -layout local -o "picnic-table-bypass-ee.pdf"