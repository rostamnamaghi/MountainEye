encoding utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

source "ld-downstream2.th"

export model -fmt survex -o "ld-downstream2-th.3d"

# export map -proj plan -layout local -o "ld-downstream2-plan.pdf"

# export map -proj extended -layout local -o "ld-downstream2-ee.pdf"