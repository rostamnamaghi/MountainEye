encoding utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

source "934LHS.th"

export model -fmt survex -o "934LHS-th.3d"

# export map -proj plan -layout local -o "934LHS-plan.pdf"

# export map -proj extended -layout local -o "934LHS-ee.pdf"