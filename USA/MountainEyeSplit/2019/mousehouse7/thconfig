encoding  utf-8
source mousehouse7.th
# Change name to the main therion file


#Surveys to output
#-----------------

export map -proj plan -layout local -o mousehouse7.pdf
#Produces plan of cave, on a single sheet

#export map -proj extended -layout local -o mousehouse7Ee.pdf

#export model -fmt survex -o mousehouse7th.3d
#Survex file showing centre line only

#export model -o mousehouse7.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o mousehouse7_Walls.3d
#Survex file showing walls only

layout local
debug off
map-header 0 0 ne
#symbol-hide group cave-centreline
endlayout
